{
  "installCommand": "corepack enable && cd dashboard && pnpm install --frozen-lockfile",
  "buildCommand": "cd dashboard && bash scripts/build-vercel.sh && mkdir -p ../.vercel && rm -rf ../.vercel/output && mv .vercel/output ../.vercel/"
}
