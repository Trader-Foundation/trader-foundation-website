# Kie.ai (kie.ai) video generation API key.
# Get one at https://kie.ai/ — never commit the real value.
KIE_API_KEY=

# Optional: override the Kie.ai API base URL (defaults to https://api.kie.ai)
# KIE_API_BASE_URL=
